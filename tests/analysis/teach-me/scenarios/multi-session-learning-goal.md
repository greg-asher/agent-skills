# Multi-session retry learning goal

The user wants to become able to redesign retry ownership safely over several sessions. The workspace model distinguishes application retries, queue delivery attempts, provider retries, and dead-letter recovery. The existing learner model has no `learningGoals` field and marks retry ownership emerging. Current provider documentation is available through a primary-source URL. The user has previously explained the application retry correctly once but has not applied the distinction across workflows.
