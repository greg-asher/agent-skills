# Topic-focused Game Show

The requested topic is queue ownership. The workspace model shows that the API publishes jobs, the worker acknowledges them, and the platform configuration owns retry policy. Evidence references exist for each relationship.
