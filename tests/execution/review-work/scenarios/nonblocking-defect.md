# Non-blocking defect

`EX-1001` is Implemented by commit `abc1001`. It promises that sellers can view a report's evidence, generation time, and source links. All seller paths and permissions pass review.

The same formatting helper also appears on an administrator diagnostics page outside the promised seller journey. For administrators in a negative UTC offset, a timestamp near midnight displays the prior calendar date while the raw instant remains correct. This is a confirmed display defect, but it does not affect seller reporting, stored data, authorization, or the issue's done conditions.
