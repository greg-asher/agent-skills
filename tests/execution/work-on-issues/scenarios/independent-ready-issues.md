# Independent ready issues

This is a disposable repository fixture. The local queue is authoritative.

## Ready

- `EX-101` Add CSV export naming to `src/export-name.ts`, with tests in `test/export-name.test.ts`.
- `EX-102` Add operator help text to `src/help.ts`, with tests in `test/help.test.ts`.

## In progress

None.

## Blocked

- `EX-103` Publish export links in the run screen. Depends on `EX-101` reaching `Done`.

## Implemented

None.

## Done

None.

The two Ready issues share no files, state, contracts, or generated output. The repository check is `npm test`.
