# Greenfield routing scenario

The folder contains a concept note for a “new” referral-triage product and an implemented Node application with routes for referral intake, a review queue, approval, rejection, and CSV export. The package manifest includes a working test command. A deployment file names the service `referral-triage-pilot`.

The concept note proposes adding automatic partner submission and a new scoring model, but calls the entire initiative greenfield.
