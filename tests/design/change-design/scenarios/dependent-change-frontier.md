# Dependent change frontier

The destination is a settled cross-region data transition design. Residency policy blocks placement. Placement blocks replication and failover choices. Current vendor capability research and internal ownership discovery can proceed independently. Compatibility and cutover decisions cannot yet be stated precisely. The design will span multiple sessions.
