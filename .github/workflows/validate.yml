name: Validate

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate marketplace, skills, and evaluations
        run: python3 scripts/validate.py

      - name: Run Kestrel plugin lifecycle tests
        run: node --test tests/kestrel/*.test.mjs
