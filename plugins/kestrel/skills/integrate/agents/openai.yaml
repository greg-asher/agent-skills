interface:
  display_name: "Integrate Kestrel Result"
  short_description: "Adopt verified isolated Kestrel work"
  default_prompt: "Use $integrate to verify and adopt a completed Kestrel result into this local branch."
policy:
  allow_implicit_invocation: false
